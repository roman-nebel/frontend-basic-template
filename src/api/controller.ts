export async function get(endpoint: string, options: Object) {
  try {
    /*
    const responce = await fetch("baseURI + endpoint", {
      method: "GET"
    })
    */
    return true
  } catch {
    return false
  }
}
